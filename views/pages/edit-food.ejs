<!DOCTYPE html>
<html lang="en">
  
<meta http-equiv="content-type" content="text/html;charset=utf-8" />
<head>
    <title>Edit Food</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <link rel="stylesheet" href="../bootstrap/css/bootstrap.min.css">
    
  </head>
  <body>
  
  <div class="container mt-3">
    <div class="col-md-12">
      <div class="card">
        <div class="card-header h3">Edit Food Details</div>
        <div class="card-body">
          <form action="update/<%= foods[0].id %>" method="POST">
            <div class="row">
              <div class="form-group col-md-6">
                <label class="mb-2">Food Name</label>
                <input type="text" name="title" class="form-control" placeholder="e.g. Chicken Laps" value="<%= foods[0].title %>" required>
              </div>
              <div class="form-group col-md-6">
                <label class="mb-2">Category</label>
                <select name="cat_id" class="form-control" required>
                  <% 
                    if(category) {
                      for(var i = 0; i < category.length; i++) {
                  %>
                  <option value="<%= category[i].id %>"><%= category[i].name %></option>
                  <% }
                  }
                  %>
                </select>
              </div>
              <div class="form-group col-md-4">
                <label class="mb-2">Price</label>
                <input type="number" name="price" class="form-control" placeholder="e.g. 100.00" value="<%= foods[0].price %>" required>
              </div>
              <div class="form-group col-md-4">
                <label class="mb-2">Image</label>
                <input type="file" name="image" class="form-control">
              </div>
              <div class="form-group col-md-4">
                <label class="mb-2">Status</label>
                <select name="status" class="form-control">
                  <option value="1">Available</option>
                  <option value="0">Not Available</option>
                </select>
              </div>
              <div class="form-group col-md-12">
                <label class="mb-2">Desciption</label>
                <textarea name="description" class="form-control"><%= foods[0].description %></textarea>
              </div>
              <div class="form-group col-md-12">
                <input type="submit" value="Update Food" name="btnUpdateFood" class="btn btn-primary mt-3">
                <a href="/foods" class="btn btn-danger mt-3">Back</a>
              </div>
              
            </div>
          </form>
        </div>
      </div>
    </div>

  </div>

  <script src="js/bootstrap/css/bootstrap.min.js"></script>
    
  </body>

</html>